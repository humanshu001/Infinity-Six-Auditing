Whatever test you perform, you need to first take the smart conmtract to the current mainnet values which are mentioned in the i6systemcontract-values.md and i6-token-values.md files


Make sure you take the tests with the real blockchain values and test it according to the current values on the blockchain, i6systemcontract-values.md and i6-token-values.md are for the values

-buying is permanently closed
-there is trading cooldown on withdrawls of one hours
-uses tex.origin which blocks contract calls
-not more than one single transaction can be in a single block by one wallet 